spring:
  application:
    name: banking-backend
  datasource:
    hikari:
      # Conservative on purpose: free-tier hosts and free-tier hosted MySQL
      # both have small memory/connection budgets. See docs/deployment.md.
      maximum-pool-size: ${DB_POOL_MAX_SIZE:5}
      minimum-idle: ${DB_POOL_MIN_IDLE:1}
      connection-timeout: 10000
      max-lifetime: 280000
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
        format_sql: false
  flyway:
    enabled: true
    baseline-on-migrate: false
  jackson:
    time-zone: UTC
    default-property-inclusion: non_null
  main:
    banner-mode: off

server:
  port: ${PORT:8080}
  error:
    include-message: never
    include-stacktrace: never
    include-binding-errors: never
    include-exception: false
  servlet:
    session:
      cookie:
        http-only: true
        name: BANKINGDEMOSESSION
        secure: ${COOKIE_SECURE:true}
        same-site: ${COOKIE_SAME_SITE:lax}

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      probes:
        enabled: true
      show-details: never

app:
  admin-bootstrap:
    username: ${ADMIN_BOOTSTRAP_USERNAME:}
    email: ${ADMIN_BOOTSTRAP_EMAIL:}
    password: ${ADMIN_BOOTSTRAP_PASSWORD:}
  demo-limits:
    max-funding-amount: ${DEMO_MAX_FUNDING_AMOUNT:100000}
    max-transfer-amount: ${DEMO_MAX_TRANSFER_AMOUNT:100000}
    daily-transfer-limit: ${DEMO_DAILY_TRANSFER_LIMIT:500000}
  rate-limit:
    login-per-minute: ${RATE_LIMIT_LOGIN_PER_MINUTE:10}
    register-per-hour: ${RATE_LIMIT_REGISTER_PER_HOUR:20}
    recovery-per-hour: ${RATE_LIMIT_RECOVERY_PER_HOUR:10}
    ai-per-minute: ${RATE_LIMIT_AI_PER_MINUTE:10}
  trust-proxy-headers: ${TRUST_PROXY_HEADERS:false}
  ai:
    # AI features are entirely optional -- leave the key blank to run without them.
    # Free Groq API key: https://console.groq.com (no credit card required).
    # Limits below sit under Groq's free plan for openai/gpt-oss-20b (verified 2026-09-19).
    groq-api-key: ${GROQ_API_KEY:}
    groq-endpoint: ${GROQ_ENDPOINT:https://api.groq.com/openai/v1/chat/completions}
    groq-model: ${GROQ_MODEL:openai/gpt-oss-20b}
    reasoning-effort: ${GROQ_REASONING_EFFORT:low}
    max-completion-tokens: ${AI_MAX_COMPLETION_TOKENS:600}
    timeout-ms: ${AI_TIMEOUT_MS:8000}
    failure-cooldown-seconds: ${AI_FAILURE_COOLDOWN_SECONDS:120}
    include-descriptions: ${AI_INCLUDE_DESCRIPTIONS:true}
    max-calls-per-minute: ${AI_MAX_CALLS_PER_MINUTE:20}
    max-calls-per-day: ${AI_MAX_CALLS_PER_DAY:800}
    max-estimated-tokens-per-minute: ${AI_MAX_TOKENS_PER_MINUTE:6000}
    max-estimated-tokens-per-day: ${AI_MAX_TOKENS_PER_DAY:150000}

---
# Local dev without Docker: `mvnw spring-boot:run` against a MySQL you run yourself
# (e.g. `docker compose up mysql` or a local MySQL Server install).
spring:
  config:
    activate:
      on-profile: local
  datasource:
    url: "jdbc:mysql://localhost:3306/banking_demo?serverTimezone=UTC&sslMode=DISABLED&allowPublicKeyRetrieval=true"
    username: banking_app
    password: change_me_local_only
server:
  servlet:
    session:
      cookie:
        secure: false

---
# Used inside docker-compose: connects to the `mysql` service by name.
spring:
  config:
    activate:
      on-profile: docker
  datasource:
    url: "jdbc:mysql://${DB_HOST}:${DB_PORT}/${DB_NAME}?serverTimezone=UTC&sslMode=${DB_SSL_MODE:DISABLED}&allowPublicKeyRetrieval=true"
    username: ${DB_USER}
    password: ${DB_PASSWORD}
server:
  servlet:
    session:
      cookie:
        secure: ${COOKIE_SECURE:false}

---
# Hosted deployment (e.g. Render app service + Aiven MySQL). Requires TLS with
# certificate + hostname verification -- see docs/deployment.md for how the CA
# certificate is turned into a truststore at deploy time.
spring:
  config:
    activate:
      on-profile: prod
  datasource:
    url: "jdbc:mysql://${DB_HOST}:${DB_PORT}/${DB_NAME}?serverTimezone=UTC&sslMode=VERIFY_IDENTITY&trustCertificateKeyStoreUrl=${DB_SSL_TRUSTSTORE_URL}&trustCertificateKeyStorePassword=${DB_SSL_TRUSTSTORE_PASSWORD}"
    username: ${DB_USER}
    password: ${DB_PASSWORD}
server:
  servlet:
    session:
      cookie:
        secure: true

---
# Used only by backend automated tests. Testcontainers supplies the real
# datasource URL/credentials dynamically (see TestcontainersConfiguration) --
# nothing hardcoded here.
spring:
  config:
    activate:
      on-profile: test
server:
  servlet:
    session:
      cookie:
        secure: false
